{
  "name": "css-concentric-sort",
  "version": "1.0.0",
  "description": "Sort properties pre-save in concentric order",
  "main": "./lib/main",
  "files": [
    "lib",
    "menus"
  ],
  "scripts": {
    "start": "atom .",
    "test": "atom --test ./spec/main-spec.js",
    "lint": "eslint lib/*.js spec/*.js"
  },
  "dependencies": {
    "css-declaration-sorter": "^3.0.0",
    "import-lazy": "^3.1.0",
    "js-yaml": "^3.11.0",
    "postcss": "^6.0.0",
    "postcss-scss": "^1.0.0"
  },
  "devDependencies": {
    "eslint": "^4.0.0"
  },
  "engines": {
    "atom": ">=1.5.0 <2.0.0"
  }
}
